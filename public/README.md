# Public Assets - Place Template Here

## 📋 Required Files

Place your Excel template here:

- **SKIN_CARE_AGENT.xlsm** - Amazon product listing template

This folder serves static assets that are accessible from the browser at `/` path.

Example: `public/SKIN_CARE_AGENT.xlsm` → Browser accesses at `/SKIN_CARE_AGENT.xlsm`

### For Development
The Vite dev server automatically serves files from this folder.

### For Production
When you run `npm run build`, these files are copied to the `dist/` folder and served by your production server.
